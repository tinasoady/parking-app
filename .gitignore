node_modules/
.expo/
dist/
web-build/
expo-env.d.ts

# Native projects generated by `expo prebuild` / `expo run:*`.
# They are regenerated from app.json + installed plugins, so they don't need to be tracked.
/android/
/ios/

# macOS
.DS_Store

# Logs
npm-debug.*
yarn-debug.*
yarn-error.*

# Local env files
.env*.local

# TypeScript
*.tsbuildinfo
